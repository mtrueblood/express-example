{{{ _editable }}}
<div class="hero">
  <h1>{{ title }}</h1>
  <p>{{ copy }}</p>
  <p>{{ cta }}</p>
  {{#if image }}
    <img src={{ image }} alt="{{ title }}" />
  {{else}}
    <p>no image provided</p>
  {{/if}}
</div>